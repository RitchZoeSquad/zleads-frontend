/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images:{
        domains:
        [
            "zleads-backend.vercel.app"
        ]
    },
}

module.exports = nextConfig
